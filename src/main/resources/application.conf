server-config {
  port = "9000"
}

postgres-db {
  dataSourceClassName=org.postgresql.ds.PGSimpleDataSource
  dataSource {
    user=postgres
    password=postgres
    url="jdbc:postgresql://pg:5432/recipes"
  }
}
